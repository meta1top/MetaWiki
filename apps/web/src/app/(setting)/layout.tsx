import type { FC, PropsWithChildren } from "react";

import { SettingLayout } from "@/components/layout/setting";

const Layout: FC<PropsWithChildren> = (props) => <SettingLayout {...props} />;
export default Layout;
